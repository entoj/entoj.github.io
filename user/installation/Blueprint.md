# Installation based on a blueprint

Create a local directory for your project
```bash
$ mkdir my-project
$ cd my-project
```

Clone the blueprint
```bash
$ git clone https://github.com/entoj/entoj-blueprint-sass-jspm .
```

Install all dependencies via npm
```bash
$ npm install
```
